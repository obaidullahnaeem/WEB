website : page displayed on web browser (written in html)
web server : computer that hosts a website on the Internet
website: collection of web pages in one 
web service : software that responds to internet requests, backed by web server > also provide web pages , includes websites 

Search engine: web service that helps you find other web pages, acesseed using web browser

 web page: refers to HTML documents


clint : connect to internet and collect data e.g phone computer laptop vaccume 
server : accept req from clint and give data e.g give html page , image , file  

cd shell-lesson-data

powershell commands 
0 pwd                  (Print Working Directory)
1  ls                  display the files 
2 cd [folder_name]     go in the file 
3 cd ..                (Move Up/Back)]
4 cd ~                 (Go Home)
5 mkdir [folder_name]    (Make Directory)  Creates a brand new, empty folder
6 touch [file_name.txt]                  What it does: Creates a blank, empty file.
7 cp [source] [destination] (Copy from one file to destination )
8 mv [source] [destination] (Move / Rename if you choose the destination )
9 rm [file_name] (permanently Remove the file )
10 rm -r [folder_name] (Remove Recursive) delete the entire folder and everything inside it 
   

  LOOKING INSIDE FILE 
11 cat [file_name]     (Concatenate)  show all the content in the file on screen
12 less [file_name]    Opens a file , scroll through it using your arrow keys, q exit back to terminal.
13 head [file_name]      Shows  first 10 lines of a file.
14 tail [file_name]      Shows  last 10 lines of a file

SSH PASSKEY>>>> obaidullah


GITHUB 
git clone <repository-url>    # Download a repository
git status                    # Check changed files
git pull                      # Get latest changes from remote
git log                       # View commit history
git branch                    # List branches
git checkout <branch>         # Switch branches


Connect to Repositry :
1.
suppose a folder is made on computer or else make dir 
2.
# Initialize Git in this folder
git init
# Add all your existing files to be tracked
git add .       ### to change all 
# Commit them with a message
git commit -m "Initial commit of existing project"/ what you changed
# Rename the default branch to 'main' (GitHub's standard)
git branch -M main
3.
 make a repository 
4.
copy the url 
5.
# Link your local folder to GitHub (Replace with YOUR actual GitHub URL)
git remote add origin https://github.com/YOUR-USERNAME/YOUR-REPO-NAME.git

# Push your files up to GitHub
git push -u origin main

obaidullah 

Start of HTML AND CSS:
 All the text, links, cards, lists, and buttons are created in HTML. 
 HTML puts information on a webpage, and CSS positions that information, gives it color, changes the font, and makes it look great!
 CSS is what adds style to those plain elements.

              HEAFINGS
 <h1> tells the browser: "Start a main heading here."
</h1> tells the browser: "The heading ends here."
              TAGS
starts paragraph <p> 
 </p>   ends it.

images                 <img>
line breaks            <br>


           ELEMENTS
element is a complete package of :           
{Opening Tag} + {Content} + {Closing Tag} = {Element}


doctype for HTML5 file is <!DOCTYPE html>

     "HEAD" 
head is the invisible part of webpage 
to talk to the browser only 
    <head>
      <meta charset="UTF-8"> 
      <title>
            My First Web Page 
            </title>
        </head>